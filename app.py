from dash import Dash, html, dcc

app = Dash(__name__)

app.layout = html.Div(children=[
                html.Div(
                    id = "sidebar-menu",
                    className="light",
                    children=[
                        html.Figure(
                            
                        )
                    ])
            ])